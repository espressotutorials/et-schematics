{
  "$schema": "http://json-schema.org/schema",
  "title": "Module Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "name": {
      "type": "string",
      "description": "The name of the routing module and component"
    },
    "path": {
      "type": "string",
      "description": "The name of the routing module and component",
      "$default": {
        "$source": "pages"
      }
    }
  }
}
